Final:
- Tutorial - Make the swipe to shift gravity shift the pieces to make a smiley face or some other picture
- Animate tutorial messages
- Reorder tutorial more elegantly
- Fix memory leak
- Make the loading screen an actual animation video, not react

would be nice:
- my hooks are all fucked up. They should actually have all of their dependencies
and the fact that they don't is a symptom of bad design
- When a dropped piece makes a space activate highlightPulse,
  that space's highlightPulse -- and only that space's highlightPulse --
  should not start until the piece has stopped animating.
  The newly made highlightPulse space(s) should start pulsing the
  next time highlightPulse.value reaches 0 after the newest piece's
  animation has fully stopped.
- can place in enemy well
- pieces double in wells sometimes on reset
- The glitch where two pieces occupy the same well sometimes occurs still
- Pieces jiggle
- If it's a tie, the person who shifted gravity's pieces light up first, then the other players pieces light up. A little "gotcha". Only then do the corners change to alternating / both colors
- Purchasable theme
- Entire app is styled by Theme
- A piece's well location is not updated when you move it to another well, causing
  an illegal move to move it to the well it was originally in, not the well
  it was in before the move
- exiting to the menu and making a new game switches who plays first
- Send game to a friend / review game (continue changes to 'review' when the game is over and you hit menu)
- Loading screen lags
- Allow pieces to be taken off the board anyone can put pieces on the board with no restrictions in postGame
- Winner aura and color fades immediately when reset is called, not once the pieces reach the well
- set winner z index to 5000 so they don't go under slots (mind the glass)

- Animated screen transitions

- Available/unavailable slot visualizations
- all slot arrows turn the direction of the pull (make a feed forward?)

- Make gravity shift exactly 1/4 of the board

- Settings page animates when theme changes

- Piece dropped on inaccessible space bounces off glass.

- Shiny sheen flashing(in sync or does the sheen appear to cross between pieces?),
  slight pulsing. Highlight undulates and shimmers.
- prevent winner intro animation from playing again, unless a new piece is added to the row

4 Player:

- turns handled differently
- can only drop from one side
  $ Available slot visual
- can only pull from one side
- rotate function
- rotate animation: long touch, pan, flick
- Board lifts up when you hold down on a corner
- Split wells so each player is near one

Tests

= Blocked piece must return to high z index because it goes under the slot rim when it goes back to the well
= moveInProgress restricts the number of moves you can make and prevents interfering wiht pieces in motion

New:

tapping a space auto-places
slot visuals
high fidelity css graphics
app store release
purchaseable skins
music
animations
free up piece grabbing a little before turn change
