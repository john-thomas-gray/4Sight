Tutorial:

- Animations of hand gestures: Long tap. Swipe. Shake.

A tutorial that runs the first time a user navigates to @gamePlay.tsx. If they don't complete the tutorial and go back to the main menu or close the app before completion, reset the tutorial. Add a toggle in settings "Tutorial" Description: An interactive tutorial explains the rules.
Tutorial:
(Each step is accompanied by a new Component "TutorialModal.tsx")
Step 1: "Drag a piece and release over a slot to drop the piece into the board."
Dims everwhere except for the 0-4 slot and its row up to the top space, and the white piece well. Waits for a player to drop a piece, then the dim fades out.
Step 2: Players take turns dropping pieces.
Press modal. Modal disappears and a black piece animates from the black piece well (mirroring the well space where the white piece was taken from) to slot 0-4 and drops on top of the white piece the player dropped.
Step 2: "Pieces can be dropped from any side of the board."
Press modal; it disappears. A white piece animates to slot 8-2 gets placed. A black piece animates to slot 0-2 and gets placed. A white piece animates to slot 8-1 and gets placed. A black piece animates to 0-5 and gets placed.
Step 3: "Swipe a direction to shift gravity and make the pieces fall that way."
Highlights top quarter triangle of the board, including slots and corners. Waits for player to swipe down and shift gravity.
Step 4: "If a space is available, you may place a piece directly on it to trigger a drop."
Dim everywhere except for space 7-2. Wait for the player to drop a piece in the slot. Undim.
Step 5: "The first player to get four in a row horizontally, vertically or diagonally wins the game!"
...As winning animation plays. Player clicks modal.
Step 6: "Shake your device at any time to restart the game."
Player shakes their device and the game resets.
Step 7: "Winner goes first. Reactivate the tutorial by visiting the settings menu. Play fair and have fun!"
Click modal, it disappears. The UseRef toggles so the turtorial will not play again unless reset in@settings.tsx

Tutorial should introduce the concepts of:

- dropping a piece from any direction
- stacking pieces in any direction
- escaping a seemingly insermountable situation by pulling (I want the classic three in a row with either side open)
- pulling for defence/offense

The automatic moves in step 2 should not move directly to the space. They should move the piece to the slot that will get them to that space, and trigger the normal slot to space animation from @pieceAnimations.ts.
Also, the pieces are placed incorrectly. The first black should be dropped in slot 0-4, the second white should be dropped in 6-0, the second black should be dropped in slot 0-4, the third white piece should be dropped in 7-8, and the third black piece should be should be dropped in 8-1.
