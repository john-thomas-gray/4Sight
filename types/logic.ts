export enum Turn {
  One = 1,
  Two = 2,
  Three = 3,
  Four = 4,
}
export enum GameMode {
  TwoPlayer = "twoPlayer",
  FourPlayer = "fourPlayer",
}
export enum GameState {
  PreGame = "preGame",
  Loading = "loading",
  Ready = "ready",
  Playing = "playing",
  Finished = "finished",
}
