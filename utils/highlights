I need to highlight certain pieces.
In order to do this, I need a highlight component attached to each piece.
The highlight is of two types:
-winner
-almost
When xInARow fires and finds 3 pieces in a row with an open space THAT IS REACHABLE as the next space,
the pieces' highlight.almost ANIMATE in and the pieces start to dance.
When xInARow fires and finds four pieces in a row:
-if both teams have a win, it returns a winner object with the winner team with two arrays where the winners' pieces are
-if one team wins it retuens a winner object with the winner team with one array where the winners pieces are
-if no team wins it returns a winner object with the winner team null with an empty array
The winner team arrays are passed to a function that highlights the winning pieces and animates them
in sequence from the piece that was dropped to the last piece in the line.
